export enum AppRouts {
  Main = '/',
  Login = 'login',
  Favorites = 'favorites',
  Property = 'property',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
